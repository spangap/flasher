# Root URL of the builds repo this flasher pulls images from.
# The flasher fetches  <root>/<branch>/<name>.zip  for  ?build=<name>, where
# <branch> comes from ?branch=<ref> (default main). For raw GitHub that branch
# is the git ref, so the root must NOT include it here.
# The first non-comment, non-blank line below is used as <root>.
#
# Raw GitHub (public repo, works with no extra setup):
https://raw.githubusercontent.com/reticulous/reticulous-builds
#
# NOTE: GitHub Pages bakes the branch into the site root and can't select a ref
# from the path, so it isn't compatible with ?branch=. Use raw GitHub above.
#
# Point this at spangap/spangap-builds instead when deploying the spangap flavor.
